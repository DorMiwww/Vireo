# OS
.DS_Store
Thumbs.db

# IDE / editors
.idea/
*.iml
.vscode/
*.swp
*.swo
.claude/

# /unlazy completion ledgers — per-session working state, not repo history
GATES.md
.unlazy/
.unlazy-hook-state.json

# Gradle build & cache outputs
.gradle/
build/
**/build/
bin/
out/

# vireo-idea-plugin consumes these as plain jars (see BLOCK.md) — derived
# artifacts, regenerate with vireo-idea-plugin/sync-libs.sh
vireo-idea-plugin/libs/*.jar

# IntelliJ plugin sandbox & temporary cache
.intellijPlatform/
**/.intellijPlatform/